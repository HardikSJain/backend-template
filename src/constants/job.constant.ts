export enum QueueName {
  EMAIL = 'email',
  OTP = 'otp',
}

export enum QueuePrefix {
  AUTH = 'auth',
}

export enum JobName {
  EMAIL_VERIFICATION = 'email-verification',
  SEND_OTP = 'send-otp',
}
